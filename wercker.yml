box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.26"
        theme: minimal
        flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: mi-bear.github.io
        basedir: public

